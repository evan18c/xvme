## XVME
XVME (Xbox Virtual Machine/Emulator) is a project designed to run XBE files.

# Building
To build the project, simply run `clang source\*.c -Iinclude -o xvme.exe -w` in the project's directory.

# Support
XVME currently does not support any titles and is more of a learning experiment.